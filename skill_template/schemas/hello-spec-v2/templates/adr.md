# {决策的简短标题}

> 日期：YYYY-MM-DD ｜ 来源提案：<change-name>（如 search-ai-board）

{1-3 句话：当时的背景是什么、我们决定了什么、为什么。}

<!--
本文件是一条【架构决策记录 ADR (Architecture Decision Record)】。
落地位置：.ai_doc/spec-workflow/adr/NNNN-<slug>.md（顺序编号，如 0001-event-sourced-orders.md）
作用：记录“当时为什么选 A 不选 B”，防止半年后有人无意识推翻一个其实深思熟虑的决策。

头部两字段是轻量元信息，必填：
- 日期：决策定下的日期，用于判断时效。
- 来源提案：这条决策源自哪个 openspec change，便于反向追溯上下文。

与 design.md 的区别（别串味）：
- design.md 记“现在方案长什么样”(how it works now)
- ADR 记“为什么这么定 + 否决了什么”(why this, not that)

懒创建 + 克制：只有【同时满足】下面三条才写 ADR，否则不写——
1. 难以回退：以后改主意代价大。
2. 没背景会困惑：未来读代码的人会问“为什么这么搞？”。
3. 是真实权衡：确实有别的可选项，你为具体理由选了这个。
任一条不满足 → 不写。绝大多数需求不会产生 ADR。

正文克制：一段话即可，价值在“记录了决策 + 为什么”，不在填满小节。
编号：扫描 adr/ 目录里最大编号 +1。
-->

<!-- 下面三节均为可选，多数 ADR 不需要，仅在确有价值时才加：

## 状态（Status）
proposed | accepted | deprecated | superseded by ADR-NNNN

## 备选方案（Considered Options）
<只有当“被否决的备选方案”值得被记住时才写>

## 后果与影响（Consequences）
<只有当存在不直观的下游影响时才写>
-->
