name: hello-spec
version: 1
language_policy:
  output_language: zh-CN
  rules:
    - All generated human-facing artifacts MUST be written in Simplified Chinese.
    - Keep code identifiers, file paths, CLI commands, API names, package names, branch names, error messages, and config keys in their original language.
    - Normative keywords such as MUST, SHALL, SHOULD, MAY can remain in English when required by spec format, but the surrounding explanation MUST be Chinese.
    - If an invoked Skill returns English content, translate and adapt it into Simplified Chinese before writing final artifacts.
    - Do not produce an all-English proposal, design, task list, or plan unless the user explicitly requests English.
description: >
  基于 Spec-driven Development 的中文工作流，集成 Superpowers skills。
  流程：brainstorm → proposal → design/specs → tasks → plan。
  apply 阶段使用 git worktrees 与 subagent-driven development 执行开发。
  默认所有文档产物使用简体中文。

artifacts:
  - id: brainstorm
    generates: brainstorm.md
    description: Collaborative design exploration using Superpowers brainstorming
    template: brainstorm.md
    instruction: |
      Use the Skill tool to invoke **superpowers:brainstorming**.

      IMPORTANT output redirection:
      - Do NOT write to `docs/superpowers/specs/`. Instead, write the
        brainstorming design output directly to this change's `brainstorm.md`.
      - If brainstorming produces a design document, also write it to
        this change's `design.md` (this will pre-fill the design artifact).

      After brainstorming completes, summarize the agreed design in
      brainstorm.md using the template structure.

      The brainstorming skill will:
      1. Explore project context
      2. Ask clarifying questions (one at a time)
      3. Propose 2-3 approaches with trade-offs
      4. Present design sections for approval
      5. Output the validated design
    requires: []

  - id: troubleshoot
    generates: troubleshoot.md
    description: Placeholder for post-implementation issue records, consumed by the /spec-trouble-resolve workflow.
    template: troubleshoot.md
    instruction: |
      Create troubleshoot.md as an empty placeholder using the template verbatim.
      DO NOT generate any issue content. This file is intentionally empty at
      proposal-creation time. Its purpose is to provide a known location for
      the user to append problem reports after implementation, which will then
      be consumed by the `/spec-trouble-resolve` command.

      Rules:
      - Copy the template content as-is. No edits, no inferred entries.
      - Do not summarize design/plan into this file.
      - Companion file `backup.md` will be created lazily by the resolve
        workflow on first archival; do NOT pre-create it here.
    requires: []

  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      Create the proposal document based on the brainstorming output.

      Read brainstorm.md for the validated design. Extract:
      - **Why**: The motivation (from brainstorming discussion)
      - **What Changes**: Specific changes agreed upon
      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names.
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Check `openspec/specs/` for existing spec names.
      - **Impact**: Affected code, APIs, dependencies, or systems.

      Since brainstorming already explored the problem space,
      this should be a concise extraction, not a new exploration.

      IMPORTANT: The Capabilities section is critical. It creates the contract between
      proposal and specs phases. Each capability listed here will need a spec file.

      Keep it concise (1-2 pages). Focus on the "why" not the "how".
    requires:
      - brainstorm
      - troubleshoot

  - id: specs
    generates: "specs/**/*.md"
    description: Detailed specifications for the change
    template: spec.md
    instruction: |
      Create specification files that define WHAT the system should do.

      Create one spec file per capability listed in the proposal's Capabilities section.
      - New capabilities: use the exact kebab-case name from the proposal (specs/<capability>/spec.md).
      - Modified capabilities: use the existing spec folder name from openspec/specs/<capability>/ when creating the delta spec at specs/<capability>/spec.md.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed behavior - MUST include full updated content
      - **REMOVED Requirements**: Deprecated features - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.

      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal

  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: |
      Create the design document explaining HOW to implement the change.

      Check if brainstorming already produced a design.md file.
      If design.md already exists with content from brainstorming,
      review and refine it rather than starting from scratch.

      If design.md is empty or doesn't exist, create it from
      the brainstorm.md output.

      Sections:
      - **Context**: Background, current state, constraints, stakeholders
      - **Goals / Non-Goals**: What this design achieves and explicitly excludes
      - **Decisions**: Key technical choices with rationale (why X over Y?). Include alternatives considered.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)
      - **Open Questions**: Outstanding decisions or unknowns to resolve

      Focus on architecture and approach, not line-by-line implementation.
      Good design docs explain the "why" behind technical decisions.
    requires:
      - brainstorm
      - specs


  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: |
      Create the task list that breaks down the implementation work.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses
      checkbox format to track progress. Tasks not using `- [ ]` won't be tracked.

      Guidelines:
      - Group related tasks under ## numbered headings
      - Each task MUST be a checkbox: `- [ ] X.Y Task description`
      - Tasks should be small enough to complete in one session
      - Order tasks by dependency (what must be done first?)
      - Do not include unit tests. The project is too large to successfully run unit tests and the go build process.

      Reference specs for what needs to be built, design for how to build it.
      Each task should be verifiable - you know when it's done.
    requires:
      - specs
      - design

  - id: plan
    generates: plan.md
    description: Micro-task implementation plan using Superpowers writing-plans
    template: plan.md
    instruction: |
      Use the Skill tool to invoke **superpowers:writing-plans**.

      IMPORTANT output redirection:
      - Do NOT write to `docs/superpowers/plans/`. Instead, write
        the plan directly to this change's `plan.md`.
      - Do not include unit tests, not allow go build execute. The project is too large to successfully run unit tests and the go build process.

      The writing-plans skill will:
      1. Read the tasks.md and design.md for context
      2. Break each task into 2-5 minute micro-steps (TDD style)
      3. Include exact file paths, code snippets, allowed write scope, and verification commands
      4. Add a lightweight `Subagent Execution Contract` section that groups micro-steps into assignable slices

      Pass the tasks.md content to the skill as the input for
      decomposition. The plan should reference the specs and
      design artifacts in this change directory.

      Subagent execution requirements:
      - The final plan.md MUST contain a non-empty `## Subagent Execution Contract`
        section.
      - Keep this lightweight; do not copy the heavier hello-spec-v2 dispatch matrix.
      - Each slice should include SliceID, TaskRefs, Allowed Write Scope, Read First,
        Acceptance, Verification, and Stop Conditions.
      - Verification MUST respect this repo's policy: do not include full `go test ./...`
        or broad `go build` commands.
    requires:
      - tasks

apply:
  requires: [plan]
  tracks: tasks.md
  instruction: |
    Note when implementing, it is forbidden to execute go build and go test. The project is too large. These operations will cause lag and memory exhaustion.
    1. **Coordinator boundary**: the main agent is only the apply coordinator.
       It must read plan.md/tasks.md, prepare subagent task packets, inspect
       returned diffs, and update ledgers. It must NOT edit production code in
       the main thread unless the user explicitly provides `SERIAL_APPLY=true`.

    2. **Executor**: Use the Skill tool to invoke **superpowers:subagent-driven-development**,
       but apply this schema's stricter prompt contract:

       - The coordinator reads plan.md first and extracts the next incomplete slice
         from `## Subagent Execution Contract`.
       - Do NOT tell a subagent to "read plan.md and figure it out". Paste the full
         slice text into the subagent prompt, including TaskRefs, exact micro-steps,
         Allowed Write Scope, Read First files, Acceptance, Verification, and Stop Conditions.
       - Each implementation subagent gets one slice only and may edit only paths
         listed in Allowed Write Scope.
       - Subagents must update plan.md checkboxes for completed micro-steps and
         tasks.md checkboxes only when all matching micro-steps for the coarse task
         are complete.
       - Subagents must not write troubleshoot.md. It is user-curated.
       - Subagents must report: Status=DONE|DONE_WITH_CONCERNS|BLOCKED|NEEDS_CONTEXT,
         files changed, verification commands run, ledger updates, and blockers.

    3. **No silent serial fallback**: if subagent dispatch is unavailable, stop before
       production-code edits with `Stopped at: subagent_mode_unavailable`. Serial
       main-agent implementation is allowed only when the user explicitly provides
       `SERIAL_APPLY=true`.

    4. **Completion**: When all tasks are done, use the Skill tool to invoke **superpowers:finishing-a-development-branch** for branch cleanup, PR creation, etc.

    5. **Diagnostics**: After code implementation is complete, invoke the gopls MCP tool's go_diagnostics to validate correctness. Any error-level diagnostics must be fixed and re-validated until all checks pass.

    6. **Troubleshoot scope**: `troubleshoot.md` is NOT written by the apply phase or any subagent. It is a user-curated file. Issues discovered during implementation must be appended manually by the developer and are later consumed by the `/spec-trouble-resolve` command. Do not let executors auto-populate it.
