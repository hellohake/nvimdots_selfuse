---
name: spec-e2e-debug
description: openspec提案apply之后、端到端测试阶段出现"现象不明、根因未知"问题时的内网增强根因定位器。输入 debug.md（现象/logid/服务名/泳道/tcc/libra 等），结合代码现状 + bytedcli 内网平台（日志/服务树/配置/实验/指标/实例/DB/RPC复现）做假设-验证式根因定位，产出四分类诊断结论（代码问题/业务逻辑问题/正常现象/边界case）+ 证据链，落盘 debug-report.md。只读不改、诊断优先。Use when 用户说"端到端测了有问题但不知道哪错、帮我查 logid、定位根因、排查线上/泳道现象、这个返回不对帮我诊断"，或 hello-spec-v2 apply 后测试出问题时。
---

# spec-e2e-debug

## 🔴 安全红线（最高优先级，先读，违反 = 线上故障）

**本技能只做"只读诊断"，绝不执行任何写操作。** 端到端测试常发生在泳道甚至线上链路，一次误写可能造成线上故障。因此：

1. **bytedcli / RPC / 复现，一律只读**：只允许 get / list / query / search / describe / 诊断类 调用。
2. **任何写操作禁止执行**：create / update / delete / set / restart / deploy / publish / rollback / 改 tcc / 改实验 / 重启实例 / 下单 / 扣减 / 状态变更 …… 一律**不执行**，只**列出命令 + 风险说明，交用户人工执行**。
3. **拿不准是否只读 → 一律当写操作处理，停手问用户**。判断不了副作用半径时，宁可不调。
4. **RPC / bam / api-test 复现的特别约束**：只允许调**幂等查询类**下游接口；任何会改下游数据的接口（即使测试环境）禁止调。
5. **调用前自检（每次内网调用前默念）**：「这个调用会改变任何状态吗？会 → 停。」

> 这条红线优先级高于一切诊断目标。诊断不出来可以问用户，但**绝不能为了诊断去写线上**。

## 🟠 环境就绪性（bytedcli 鉴权 / 版本）—— 诊断前先确保工具能用

bytedcli 依赖内网鉴权和工具版本，**鉴权过期 / 工具过旧是高频问题**。环境类失败**不是诊断信号**，绝不能误当成业务结论（`auth status` 失败 ≠ 被测服务挂了）。

**预检（阶段 0 之前先做一次，开销极小）**：
- 跑 `mcp__bytedcli__run_command(command="auth status")` 确认已登录；未登录/过期 → **停下来提示用户手动执行 `bytedcli auth login`**，不继续诊断。

**运行中失败分类处置**（任何 bytedcli 调用失败，先分类，别当诊断信号）：

| 失败类型 | 特征关键词 | 处置 |
|---|---|---|
| 鉴权失败 | auth / 401 / token expired / not logged in | **停**，提示用户手动 `bytedcli auth login`，**不重试、不自动登录** |
| 工具过期 | version / update required / unknown command | **停**，提示用户手动 `bytedcli self update`，**不重试、不自动更新** |
| 权限不足 | permission denied / 无权访问 | 提示该平台需申请权限，**跳过此条验证路径，换其他假设继续** |
| 真·诊断信号 | 业务报错 / 数据为空 / 返回异常 | **这才是诊断要的**，正常分析 |
| 非预期报错 | 上述都不是、看不懂的错误 | **如实反馈给用户**，不臆测、不硬闯 |

**红线**：
- 🚫 **不自动跑 `auth login` / `self update`**（可能需浏览器交互 / 有副作用）——只提示，由用户手动执行。
- 🚫 **环境失败不重试**（重试照样失败，白烧时间）。
- 🚫 **环境失败绝不写进 debug-report 的业务结论**——它是工具问题，不是被测服务的问题。

## 角色与目标

你是严谨的资深 SRE + 服务端工程师。处理**端到端测试阶段**的疑难：代码已写完（apply 完成），测试时出现现象，但**不确定根因在哪**。你的目标是**结合代码现状 + bytedcli 内网平台**，用**假设-验证**的方式定位根因，给出**四分类诊断结论 + 证据链**，落盘到提案目录。

**定位（与邻居的边界）**：
- 本技能 = **诊断器**，产出"是什么问题 / 为什么"，**不改代码**。
- 诊断完若结论是代码问题 → 提示用户去跑 `/spec-trouble-resolve`（那个才改代码）。
- 与通用 `diagnose`/`systematic-debugging` 的区别：本技能**懂内网**，用 bytedcli 拉真实日志/配置/数据/实验来验证假设，而非纯方法论。

## 用户输入

- `$1`：提案名或绝对路径（必填）。
- **诊断输入（主）**：提案目录下的 `debug.md`——现象描述、logid、服务名(psm)、泳道环境、tcc key、libra 实验、复现步骤等。
- `$ARGUMENTS`（辅）：临时补充现象。
- 若 `debug.md` 不存在：提示用户在提案目录创建并填写（可参考模板），或用户口述、你代填一份 debug.md 再开始。**不臆测现象**。

## 执行工作流 (SOP)

### 阶段 0：环境预检 + 读现象 + 代码现状（遵守上下文纪律）
0. **环境预检**：先按上方「环境就绪性」跑 `auth status`；鉴权失败 → 停，提示用户 `bytedcli auth login`，不进入诊断。
1. 解析 `$1` 定位提案目录（找不到立即问，禁止盲目 Glob）。
2. 读 `debug.md` 提取关键诊断信息：logid / psm / 泳道 / tcc / libra / 现象 / 期望。**至少要有 logid 或 (服务名+时间)**，否则无法定位到具体请求/实例 → 让用户补。
3. **按需**读相关代码（守上下文纪律，只读与现象相关的函数，做证据摘要含路径+行号）。

### 阶段 1：提假设（不堆砌信息，假设驱动）
基于现象 + 代码现状，列 **2-3 个最可能的根因方向**，按可能性排序。诊断是验证假设，不是把所有平台数据拉一遍。

### 阶段 2：按诊断决策树验证（用 bytedcli，只读）
针对每个假设，选对应平台验证。**平台→意图→只读命令样例见 [references/bytedcli-debug-map.md](./references/bytedcli-debug-map.md)**（按需读，不要一次性全加载）。八类诊断路径：

| 诊断意图 | 平台 |
|---|---|
| 按 logid 拉链路日志 | slardar / log |
| 服务实例/上下游/泳道定位 | bytetree / sd / env |
| 配置值核对 | tcc |
| 实验命中诊断 | libra / sip |
| 指标/打点异常 | apm / grafana / slardar |
| 开发机实例日志 | tce / bytebox |
| DB 真实数据 | rds / bytehouse / hive / tqs |
| 下游 RPC 真实返回 / 复现 | bam / api-test |

#### 🔑 查日志的强制手法（防上下文爆炸）
一个链路日志可能几万行，**绝不把整份日志读进上下文**：
1. **先窄后宽**：只按 logid + **出问题的关键 psm** 拉，不要一上来拉所有上下游。
2. **带时间窗 + 条数上限**拉取，避免一次拉爆。
3. **落本地临时文件**：把拉到的日志写到 `/tmp/e2e-debug-<logid>/<psm>.log`。
4. **在文件里检索，只读命中摘要**：用 grep/检索找报错行、关键字段、断点，**只把命中的几行（含行号）读进上下文**，主线程绝不内联整份日志。
5. 需要扩链路再按需拉下一个 psm，重复上述。

### 阶段 3：收敛定性（四分类 + 证据链）
把验证结果收敛成**根因结论**，归入四类之一，每条结论**必须挂证据**（哪个平台查到什么、日志哪行、配置什么值）：

| 分类 | 含义 | 出口 |
|---|---|---|
| **代码问题** | 代码逻辑/边界/空指针等缺陷 | → 建议跑 `/spec-trouble-resolve` 修 |
| **业务逻辑问题** | 代码按 spec 跑了，但 spec/设计本身有问题 | → 建议回 design 澄清（spec-plan-revise）或确认需求 |
| **正常现象** | 行为符合预期，是误判 | → 说明为什么正常，无需改 |
| **边界 case** | 特定输入/时序/配置下的边界 | → 说明触发条件，用户决定是否覆盖 |

### 阶段 4：落盘 + 出口指针
1. 写诊断报告到 `<提案目录>/debug-report.md`（格式见下）。
2. 临时日志文件路径在报告里注明（便于复查），不删（用户可自行清理 /tmp）。
3. 回执（不在对话重复报告正文）：根因一句话 + 四分类结论 + 关键证据 + 下一步出口。

## debug-report.md 格式

```markdown
# 端到端诊断报告 - {提案名}
> 输入：debug.md（logid: xxx / psm: xxx / 泳道: xxx）｜诊断时间：YYYY-MM-DD HH:mm

## 现象
> 原样保留 debug.md 的现象描述

## 假设与验证
| # | 假设 | 验证平台 | 证据（含日志行/配置值/返回） | 结论 |
|---|------|----------|------------------------------|------|
| 1 | … | slardar | /tmp/e2e-debug-xxx/psm.log:1234 报错 X | 成立/排除 |

## 根因结论
- **分类**：代码问题 / 业务逻辑问题 / 正常现象 / 边界case
- **根因**：一句话讲清
- **证据链**：逐条列支撑证据（平台 + 具体数据）

## 下一步
- （代码问题）建议跑 `/spec-trouble-resolve` 修复
- （业务逻辑）建议回 design 澄清 / spec-plan-revise
- （正常/边界）说明 + 是否需补 case

## 附：本次执行的只读 bytedcli 命令
> 列出所有执行过的查询命令（便于复查）

## 附：需人工执行的写操作（如有）
> 任何诊断中发现"需要写操作才能进一步确认/修复"的，列命令 + 风险，交用户人工执行
```

## 关键红线 (Hard Rules)
1. 🔴 **只读不写**（见顶部安全红线）：写操作禁止执行，拿不准当写处理，列命令交人工。
2. 🔴 **日志落盘不内联**：关键 psm 日志写 /tmp 临时文件，只读命中摘要，绝不整份内联。
3. **诊断优先不改代码**：改代码是 spec-trouble-resolve 的事；本技能只产结论。
4. **假设驱动**：先提假设再按需查，不堆砌全平台数据。
5. **证据可追溯**：每条结论必须挂具体证据（平台+数据+行号），禁止凭感觉下结论。
6. **不臆测现象**：debug.md 信息不足（缺 logid/服务名等）先问用户。
7. **环境失败不当诊断信号**：鉴权/版本/权限类失败只提示用户手动处理（auth login / self update / 申请权限），不重试、不自动执行、不写进业务结论；非预期报错如实反馈用户。
